import { BrowserRouter, Route, Routes } from 'react-router-dom'

export const Routing = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route path='/' />
      </Routes>
      <Routes />
    </BrowserRouter>

  )
}
